import Button from './Button.vue';
import ButtonGroup from './ButtonGroup.vue';
import { withInstall } from '@pc-element/utils';

export const PcButton = withInstall(Button);
export const PcButtonGroup = withInstall(ButtonGroup);

export * from './types';
